--- a/.github/workflows/close-out-release.yml
+++ b/.github/workflows/close-out-release.yml
@@ -68,6 +68,11 @@
     steps:
       - uses: actions/checkout@v6
         with:
+          # devaudit-installer#676 (DEVAUDIT-004) — self-hosted runners share
+          # one fixed workspace per repo across every workflow; default
+          # clean:true wipes node_modules between runs, defeating the
+          # npm-ci cache entirely. No-op on ephemeral github-ci runners.
+          clean: ${{ (inputs.runner_label || vars.CI_RUNNER_LABEL || 'github-ci') == 'github-ci' && 'true' || 'false' }}
           ref: develop
           fetch-depth: 0
           token: ${{ secrets.INSTALLER_DISPATCH_TOKEN || github.token }}
