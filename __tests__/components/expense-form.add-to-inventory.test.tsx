/**
 * @requirement REQ-034 — AC5
 * Expense form's "Add to inventory" dropdown visible only for Direct Cost.
 * Options filtered to kind:'kitchen-ingredient'.
 *
 * STUB: filled in during Phase A tests-first commit.
 */
import { describe, it } from 'vitest';

describe.skip('REQ-034 AC5 — ExpenseForm Add-to-inventory dropdown', () => {
  it('shows the dropdown when expense type is Direct Cost', () => {});
  it('hides the dropdown when expense type is Repairs', () => {});
  it('hides the dropdown when expense type is Salaries', () => {});
  it('lists only kind:kitchen-ingredient inventory rows', () => {});
  it('groups options by COGS category', () => {});
});
