/**
 * @requirement REQ-034 — AC2
 * Customer-facing menu queries filter Inventory.kind = 'menu-item'.
 *
 * STUB: filled in during Phase A tests-first commit.
 */
import { describe, it } from 'vitest';

describe.skip('REQ-034 AC2 — category-service kind filter', () => {
  it('listCategories never returns kitchen-ingredient inventory', () => {});
  it('getCategoryWithItems applies kind filter', () => {});
  it('getMenuItemsForOrder excludes kitchen-ingredient kind', () => {});
});
