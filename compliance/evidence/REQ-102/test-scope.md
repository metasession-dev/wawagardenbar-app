# Test scope — REQ-102

Risk class: **HIGH** (core revenue capability — this REQ changes the price actually charged at order time).

| AC  | Description                                                                                                                                                                   | SRS item                                                              | Verification method                                                   |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | --------------------------------------------------------------------- |
| AC1 | Super-admin can enter/save default, show, and happy-hour price on the menu item edit page's Price Management section; each save appends a labelled price-history row.         | REQ-MENUMGT-008 (new)                                                 | Unit (`PriceHistoryService`) + E2E (form interaction, history viewer) |
| AC2 | Super-admin can independently configure the Show Price Window and Happy Hour Window (enable + start/end) in Settings without affecting Business Hours.                        | REQ-SETTINGS-001 (existing — updated)                                 | Unit (`/api/settings` route) + E2E (settings form)                    |
| AC3 | Happy-hour price is charged whenever the happy-hour window is active, regardless of show-price window state.                                                                  | REQ-ORDER-006 (new)                                                   | Unit (`resolveActivePriceField`, `reconcileAndValidateOrderLines`)    |
| AC4 | Show price is charged when only the show-price window is active; default price is charged when neither window is active.                                                      | REQ-ORDER-006 (new)                                                   | Unit (`resolveActivePriceField`, `reconcileAndValidateOrderLines`)    |
| AC5 | Manual price override always wins over window-resolved price.                                                                                                                 | REQ-ORDER-006 (new)                                                   | Unit (`reconcileAndValidateOrderLines`)                               |
| AC6 | Public menu display shows the server-resolved active price, not the default price, when a window is active.                                                                   | REQ-MENU-008 (new)                                                    | E2E (public menu page)                                                |
| AC7 | "Edit All" page shows every menu item with editable cost/default/show/happy-hour price, name, main category, category, availability, and filters by main category + category. | REQ-MENUMGT-009 (new)                                                 | E2E (new page)                                                        |
| AC8 | Edits on the "Edit All" page persist via the same audited price-history convention as the single-item form.                                                                   | REQ-MENUMGT-009 (new)                                                 | Unit (bulk server action) + E2E                                       |
| AC9 | Existing menu items are backfilled with `showPrice`/`happyHourPrice` equal to `price`; unaffected code paths keep working when no window is enabled.                          | @srs-deferred: migration mechanics, not new user-observable behaviour | Unit (migration script) + manual smoke                                |
